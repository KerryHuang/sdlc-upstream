# {功能名稱} 功能調整前端規格書

<!--
  文件定位：
  - 本文件為「功能調整前端規格書」，只記錄前端 UI 的異動部分
  - 前置文件：功能調整需求文件（CR-FRD）定義了 What 和 Why
  - 搭配文件：功能調整後端規格書（CR-BFS），同次調整模式產出
  - 本文件定義：前端如何呈現並操作 CR-BFS 定義的 API 變更

  核心原則：
  1. 只寫「新增、修改、移除」的畫面元件、流程、欄位
  2. 明確標記每個項目的類型：[+] 新增 / [~] 修改 / [-] 移除
  3. 標示 Breaking Change（需要前端強制更新的項目）
  4. 不重複原始 FFS 中未變更的內容

  命名規範：
  - 文件編號：CR-FFS-{模組代號}-{序號}
  - 文件路徑：docs/{模組名稱}/{功能名稱}/{功能名稱}_功能調整前端規格書_{ticket}.md
    例：docs/採購模組/採購收料/採購收料_功能調整前端規格書_MP-693.md

  圖示說明：
  - [+] 新增：本次新增的項目
  - [~] 修改：本次修改的項目
  - [-] 移除：本次移除的項目
  - ⚠️ Breaking Change：必須更新，否則功能異常
-->

---

## 版本資訊

| 版本 | 日期 | 作者 | 說明 |
|------|------|------|------|
| 1.0 | YYYY-MM-DD | {作者} | 初始版本 |

---

## 文件資訊

| 項目 | 內容 |
|------|------|
| 文件編號 | CR-FFS-{模組代號}-{序號} |
| Linear Ticket | {MP-XXX} |

---

## 參照文件

| 項目 | 內容 |
|------|------|
| 原始前端規格書 | `docs/{模組名稱}/{功能名稱}/{功能名稱}前端功能規格書.md`（FFS-{模組代號}-{序號}，版本 {N.M}） |
| 原始後端規格書 | `docs/{模組名稱}/{功能名稱}/{功能名稱}後端功能規格書.md`（BFS-{模組代號}-{序號}，版本 {N.M}） |
| 功能調整需求文件 | `docs/{模組名稱}/{功能名稱}/{功能名稱}_功能調整需求文件_{ticket}.md`（CR-FRD-{模組代號}-{序號}） |
| 功能調整後端規格書 | `docs/{模組名稱}/{功能名稱}/{功能名稱}_功能調整後端規格書_{ticket}.md`（CR-BFS-{模組代號}-{序號}） |

> **重要**：本文件僅記錄「異動部分」。未列出的原始 FFS 畫面、元件、流程維持不變。

---

## 1. 變更摘要

### 1.1 變更說明

{一段話說明本次前端變更的整體目的}

### 1.2 異動清單

| 類型 | 項目 | 頁面/元件 | 說明 |
|------|------|----------|------|
| [+] 新增 | {按鈕/欄位/頁面} | {頁面名稱} | {說明} |
| [~] 修改 | {元件名稱} | {頁面名稱} | {說明} |
| [-] 移除 | {按鈕/欄位} | {頁面名稱} | {說明} |

### 1.3 Breaking Change 評估

| 異動項目 | 是否 Breaking Change | 說明 |
|---------|---------------------|------|
| {項目} | ⚠️ 是 / ✅ 否 | {若是，說明為何前端必須更新} |

---

## 2. 畫面異動

<!--
只描述有變更的畫面。未變更的畫面請參照原始 FFS。
針對每個有異動的頁面，用 [+]/[~]/[-] 標記變更項目。
-->

### 2.1 {頁面名稱}（列表頁面）

**異動項目：**

| 類型 | 元件 | 說明 |
|------|------|------|
| [+] 新增 | {欄位/按鈕名稱} | {說明} |
| [~] 修改 | {欄位/按鈕名稱} | {說明，如：文字變更、位置調整} |
| [-] 移除 | {欄位/按鈕名稱} | {說明} |

**異動後畫面示意（只標示變更部分）：**

```
┌──────────────────────────────────────────────────────────┐
│ 頁面標題                           [新增] [+新按鈕] [匯出] │  ← [+] 新增按鈕
├──────────────────────────────────────────────────────────┤
│ 搜尋條件區                                               │
│ ┌────────────┐ ┌─────────────────┐ ┌────────────┐ [搜尋] │
│ │ 起始日期   │ │ [+] 新篩選條件  │ │ 關鍵字     │ [清除] │  ← [+] 新增篩選欄位
│ └────────────┘ └─────────────────┘ └────────────┘         │
├──────────────────────────────────────────────────────────┤
│ ┌─────┬─────────┬─────────┬──────────────┬───────┬──────┐│
│ │ □   │ 單號    │ 日期    │ [+] 新欄位   │ 金額  │ 操作 ││  ← [+] 新增表格欄位
│ └─────┴─────────┴─────────┴──────────────┴───────┴──────┘│
└──────────────────────────────────────────────────────────┘
```

### 2.2 {頁面名稱}（新增/編輯頁面）

**異動項目：**

| 類型 | 元件 | 說明 |
|------|------|------|
| [+] 新增 | {欄位名稱}（必填） | {說明} |
| [~] 修改 | {欄位名稱} | {原為可選 → 改為必填 ⚠️} |
| [-] 移除 | {欄位名稱} | {說明} |

**異動後表單示意（只標示變更部分）：**

```
┌──────────────────────────────────────────────────────────┐
│ {功能名稱} - 新增/編輯                        [儲存] [取消] │
├──────────────────────────────────────────────────────────┤
│ 基本資訊                                                 │
│ ┌─────────────────────┐  ┌─────────────────────────────┐ │
│ │ 原有欄位 *          │  │ [+] 新增欄位 *              │ │  ← [+] 新增欄位
│ │ [現有資料]          │  │ [▼ 選擇]                    │ │
│ └─────────────────────┘  └─────────────────────────────┘ │
│ ┌─────────────────────┐                                   │
│ │ [~] 原可選欄位 * ⚠️ │                                   │  ← [~] 從可選改為必填
│ │ [輸入]              │                                   │
│ └─────────────────────┘                                   │
└──────────────────────────────────────────────────────────┘
```

---

## 3. 操作流程異動

<!--
只描述有變更的操作流程。未變更的流程請參照原始 FFS。
-->

### 3.1 [+] 新增操作流程：{流程名稱}

```mermaid
flowchart TD
    A[觸發點：{如何進入此流程}] --> B[{步驟1}]
    B --> C{判斷條件}
    C -->|是| D[{步驟2}]
    C -->|否| E[{例外處理}]
    D --> F[呼叫 CR-BFS API: {端點}]
    F --> G{API 回應}
    G -->|成功| H[顯示成功訊息]
    G -->|失敗| I[顯示錯誤訊息]
```

### 3.2 [~] 修改操作流程：{流程名稱}

**原流程（參照原始 FFS §{章節}）**

**修改說明：** {說明哪個步驟被修改，以及如何修改}

**修改後的差異步驟：**

```mermaid
flowchart TD
    A[{修改後的步驟}] --> B[{新增的判斷}]
    B -->|新增路徑| C[呼叫新 API: {端點}]
    B -->|原有路徑| D[原有流程繼續]
```

---

## 4. 欄位驗證規則異動

<!--
只寫新增或修改的驗證規則。未異動的規則請參照原始 FFS。
-->

### 4.1 新增欄位驗證

| 欄位名稱 | 欄位代碼 | 必填 | 驗證規則 | 錯誤訊息 |
|---------|---------|------|----------|----------|
| [+] {欄位名稱} | `{fieldCode}` | ✅ | {驗證規則} | {錯誤訊息} |

### 4.2 修改欄位驗證

| 欄位名稱 | 欄位代碼 | 原驗證 | 修改後驗證 | 備註 |
|---------|---------|--------|------------|------|
| [~] {欄位名稱} | `{fieldCode}` | 可選 | 必填 ⚠️ | {修改原因} |
| [~] {欄位名稱} | `{fieldCode}` | 最大 {N} 字元 | 最大 {M} 字元 | {修改原因} |

---

## 5. API 對接異動

<!--
對應 CR-BFS 的 API 異動，說明前端如何呼叫新/修改的端點。
-->

### 5.1 API 對接清單異動

| 類型 | 操作 | HTTP 方法 | API 端點 | 說明 |
|------|------|----------|---------|------|
| [+] | {操作名稱} | {Method} | {端點（來自 CR-BFS）} | {說明} |
| [~] | {操作名稱} | {Method} | {端點} | {說明，e.g. Request 新增欄位} |
| [-] | {操作名稱} | {Method} | {原端點} | 已移除，請改用 {新端點} |

### 5.2 Request 異動

#### [+] 新增呼叫：{操作名稱}

```json
{
  "{newField}": "{value}",
  "{existingField}": "{value}"
}
```

#### [~] 修改 Request：{操作名稱}

**新增必填欄位（⚠️ Breaking Change）：**

```json
{
  "{existingField}": "{value}",
  "{newRequiredField}": "{value}"
}
```

### 5.3 Response 異動處理

| 類型 | 欄位 | 說明 | 前端處理 |
|------|------|------|----------|
| [+] | `{newResponseField}` | {說明} | 新增對應的 UI 顯示邏輯 |
| [-] | `{removedField}` | 已從 Response 移除 | 移除相關顯示元件 ⚠️ |

---

## 6. 使用者操作業務邏輯異動

<!--
只寫有變更的業務邏輯（連動、計算、狀態控制）。
-->

### 6.1 [+] 新增連動邏輯：{連動名稱}

| 觸發欄位 | 連動欄位 | 連動規則 |
|---------|---------|----------|
| {觸發欄位} | {連動欄位1} | {規則說明} |
| {觸發欄位} | {連動欄位2} | {規則說明} |

### 6.2 [~] 修改狀態控制

**異動的欄位啟用/停用條件：**

| 條件 | 欄位 | 原狀態 | 修改後狀態 |
|------|------|--------|------------|
| {條件} | {欄位名稱} | 啟用 | 停用 ⚠️ |

---

## 7. 驗收標準

### 7.1 新增功能驗收

| 編號 | 驗收項目 | 預期結果 | 優先級 | 通過 |
|------|----------|----------|--------|------|
| CFFA-01 | {新增功能} | {預期結果} | 高 | [ ] |

### 7.2 修改功能驗收

| 編號 | 驗收項目 | 預期結果 | 優先級 | 通過 |
|------|----------|----------|--------|------|
| CFUA-01 | {修改功能} | {預期結果} | 高 | [ ] |

### 7.3 向後相容性驗收

| 編號 | 驗收項目 | 預期結果 | 通過 |
|------|----------|----------|------|
| CFBC-01 | 原有畫面功能 | 未異動的功能正常運作 | [ ] |
| CFBC-02 | Breaking Change 項目 | 已知 Breaking Change 均已對應處理 | [ ] |

---

## 附錄

### A. 相關文件

| 文件 | 說明 |
|------|------|
| [原始前端功能規格書](../../{模組名稱}/{功能名稱}/{功能名稱}前端功能規格書.md) | 完整原始規格 |
| [功能調整後端規格書](./{功能名稱}_功能調整後端規格書_{ticket}.md) | 對應後端規格 |
| [功能調整需求文件](./{功能名稱}_功能調整需求文件_{ticket}.md) | 業務需求 |

### B. 元件說明

| 圖示 | 說明 |
|------|------|
| [+] | 本次新增 |
| [~] | 本次修改 |
| [-] | 本次移除 |
| ⚠️ | Breaking Change，前端必須對應處理 |
| ✅ | 向後相容 |
